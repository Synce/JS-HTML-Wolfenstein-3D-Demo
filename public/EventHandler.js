export default class EventHandler {
    constructor(player) {
        this.player = player
        this.events = [];

    }

    pushEvent(event) {
        this.events.push(event);
    }

    checkForEvents() {
        for (let x = -1; x < 2; x++) {
            for (let y = -1; x < 2; y++) {

            }
        }

    }
}